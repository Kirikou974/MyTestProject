using SharpDX.DirectInput;
using System;
using System.Collections.Generic;
using VTOLVRControlsMapper;

namespace ConsoleApp1
{
    class Program
    {
        static void Main(string[] args)
        {
            //Controls.LoadMappings("VTOLVRControlsMapper", "FA26B", @"F:\Steam\SteamApps\common\VTOL VR\VTOLVR_ModLoader\dev\My Mods\VTOLVRControlsMapper\mapping.FA26B.json");
            //List<IControlMapping> mappings = Controls.Mappings;
            //DirectInput di = new DirectInput();
            //Keyboard kb = new Keyboard(di);
            //kb.Properties.BufferSize = 128;
            //kb.Acquire();
            //bool poll = true;
            //while (poll)
            //{
            //    kb.Poll();
            //    var datas = kb.GetBufferedData();
            //    foreach (KeyboardUpdate state in datas)
            //    {
            //        Console.WriteLine(string.Format("Key : {0}, IsPressed : {1}, IsReleased : {2}, RawOffset : {3}", state.Key, state.IsPressed, state.IsReleased, state.RawOffset));

            //        if (state.Key == Key.Escape)
            //        {
            //            poll = false;
            //        }
            //    }
            //}
        }
    }
}
